## 📘 Архитектура таблиц в проекте `crusher-parts`

Документ фиксирует полную структуру создания таблиц и объясняет, как использовать существующие файлы и компоненты.

---

### ✅ Ядро таблиц (не трогать, работает стабильно)

| Файл               | Назначение                                 |
| ------------------ | ------------------------------------------ |
| `BaseTable.jsx`    | Универсальная таблица                      |
| `EditableRow.jsx`  | Строка таблицы в режиме редактирования     |
| `EditableCell.jsx` | Ячейка таблицы (редактирование / просмотр) |
| `ActionIcons.jsx`  | Кнопки действий в строке                   |

---

### 🧠 Централизация полей

| Файл                  | Назначение                                                   |
| --------------------- | ------------------------------------------------------------ |
| `fieldSchemas.js`     | Описание всех доступных полей (type, title, required и т.п.) |
| `fieldRenderers.jsx`  | Как отображать и редактировать значение по типу              |
| `tableDefinitions.js` | Устаревший централизованный список колонок |

**Пример использования:**

```js
export const usersTableColumns = [
  { field: 'username', ...userFields.username },
  { field: 'email', ...userFields.email },
  { field: 'role_slug', ...userFields.role_slug },
]
```

На практике колонки могут быть описаны прямо в файле таблицы или как свойство в `fieldSchemas.js` (например, `fieldSchemas.tnved_code.columns`).

---

### 🔁 Хуки и API

| Файл               | Назначение                                             |
| ------------------ | ------------------------------------------------------ |
| `useTableData.jsx` | Загружает данные, обрабатывает onAdd, onSave, onDelete |
| `useRoles.js`      | Загружает роли для выпадающих списков                  |

---

### 📄 Страницы и структура

Каждая вкладка оформляется так:

| Файл             | Назначение                                 |
| ---------------- | ------------------------------------------ |
| `UsersPage.jsx`  | Страница, подключающая вкладку             |
| `UsersMain.jsx`  | Контейнер: размещает таблицы, кнопки       |
| `UsersTable.jsx` | Компонент таблицы (подключает `BaseTable`) |

---

### 🧩 Обёртки и интерфейс

| Файл                  | Назначение                                         |
| --------------------- | -------------------------------------------------- |
| `TabRendererPage.jsx` | Проверяет доступ к вкладке, отображает её название |
| `PageWrapper.jsx`     | Оборачивает контент страницы с отступами           |
| `TableWrapper.jsx`    | Оборачивает таблицу (тень, заголовок)              |
| `TableToolbar.jsx`    | Поиск, фильтры, импорт, экспорт                    |
| `TableFooter.jsx`     | Пагинация                                          |

---

### 📥 Импорт из Excel

| Файл               | Назначение                                  |
| ------------------ | ------------------------------------------- |
| `ImportModal.jsx`  | Диалоговое окно импорта                     |
| `entitySchemas.js` | Настройки импорта: поля, шаблоны, валидация |
| `importHelpers.js` | Проверка строки перед отправкой             |

---

### 🧾 Отображение значений

| Файл                    | Назначение                            |
| ----------------------- | ------------------------------------- |
| `ValueDisplay.jsx`      | Универсальное отображение по типу     |
| `FullAddressField.jsx`  | Отображение адреса с иконкой          |
| `PlaceAddressInput.jsx` | Адрес с автозаполнением (Google Maps) |
| `fieldLabels.js`        | Названия полей для истории и тултипов |

---

### 🧰 Утилиты

| Файл                | Назначение                               |
| ------------------- | ---------------------------------------- |
| `confirmAction.js`  | Подтверждение действия через SweetAlert2 |
| `fetchBankByBic.js` | Получение данных банка по БИК            |
| `loadGoogleMaps.js` | Загрузка Google Maps API                 |
| `textUtils.js`      | `generateTabName()` — делает slug        |

---

## 🔨 Шаблон создания новой таблицы

1. 📘 В `fieldSchemas.js` описать поля или определить `columns` прямо в компоненте
2. 🧩 В `UsersTable.jsx` или аналог — подключить `useTableData` и `BaseTable`
3. 📄 В `UsersMain.jsx` — разместить таблицу
4. 📂 В `UsersPage.jsx` — подключить `UsersMain`

Готово ✅ Таблица будет поддерживать:

* добавление, редактирование, удаление
* required-поля
* выпадающие списки
* отображение истории изменений (если передан `onShowLogs`)
* строку добавления
* подтверждение удаления

---

## 📎 Рекомендации

* Не редактировать `BaseTable`, если не уверен
* Добавлять поля **только** в `fieldSchemas.js`
* Использовать `fieldRenderers` вместо ручного `display` / `editor`
* Ранее использовался `tableDefinitions.js`, теперь колонки описываются рядом с компонентом
* Если поведение уникальное (иконки, drag’n’drop) — делай локально в таблице
* Для импорта использовать `ImportModal` и схему из `entitySchemas`

---

Файл можно дополнять. Это твоя опорная точка 🧭
