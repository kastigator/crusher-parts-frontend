import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import{fileURLToPath}from'node:url'
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8')
test('Dispatch route and primary navigation are capability gated',()=>{const router=read('src/router/AppRouter.jsx'),sidebar=read('src/components/Sidebar.jsx');assert.match(router,/path="dispatch-delivery"/);assert.match(router,/capability="dispatch_delivery\.access"/);assert.match(sidebar,/can\("dispatch_delivery\.access"\)/)})
test('Workspace exposes every canonical Wave 10 work queue and boundaries',()=>{const page=read('src/pages/DispatchDeliveryWorkspacePage.jsx');for(const label of ['Готово к отгрузке','Заказы на отгрузку','Комплектация','Упаковки','Отправления','Подтверждения доставки','Исключения','Правила раздела'])assert.match(page,new RegExp(label));assert.match(page,/Передать перевозчику/);assert.match(page,/Исполнение подтверждается только фактически доставленным количеством/);assert.match(page,/Данные пока отсутствуют/)})
test('Frontend commands use only Dispatch Delivery API',()=>{const api=read('src/features/dispatchDelivery/api/dispatchDeliveryApi.js');for(const command of ['/orders','picking-requests','/packages','/shipments','delivery-confirmations'])assert.match(api,new RegExp(command));assert.doesNotMatch(api,/contract-domain|financial-operations|warehouse-inventory/)})
