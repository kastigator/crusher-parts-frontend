import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import{fileURLToPath}from'node:url'
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8')
test('After Sales route and sidebar are capability gated',()=>{const router=read('src/router/AppRouter.jsx'),sidebar=read('src/components/Sidebar.jsx');assert.match(router,/path="after-sales"/);assert.match(router,/capability="after_sales\.access"/);assert.match(sidebar,/can\("after_sales\.access"\)/)})
test('Workspace is task oriented around claims affected items trace evidence resolution and history',()=>{const page=read('src/pages/AfterSalesWorkspacePage.jsx');for(const label of ['Открытые рекламации','Доставленные позиции / новая рекламация','Затронутые позиции','Прослеживаемость','Подтверждения и расследование','Решение','Поставщик и последующие действия','Закрытые / история','История'])assert.match(page,new RegExp(label))})
test('UI states no-reopen and no-fabrication boundaries',()=>{const page=read('src/pages/AfterSalesWorkspacePage.jsx');assert.match(page,/Рекламация не открывает завершённый договор или заказ повторно/);assert.match(page,/Если звена прослеживаемости нет, система явно показывает разрыв и не придумывает связь/)})
test('Frontend commands are confined to After Sales API',()=>{const api=read('src/features/afterSales/api/afterSalesApi.js');for(const route of ['/after-sales/cases','/evidence','/investigation','/resolutions','/supplier-escalations','/downstream-handoffs','/close','/reopen'])assert.match(api,new RegExp(route));assert.doesNotMatch(api,/contract-domain|completion-lifecycle|warehouse-inventory|dispatch-delivery|financial-operations|procurement-execution/)})
